const { isAuthenticated } = require("../Middlewares/Authentication.Validation");
const {AddOrderTocart} = require("../controller/cart.controller")
const cart = async (app) => {
//   const response = await AddorderToCart();

// app.get("/ecomm.api/v1/carts" , isAuthenticated , AddOrderTocart)
};
